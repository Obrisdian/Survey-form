import styled from "@emotion/styled";
import { ContainerProps, TextField } from "@mui/material";
import { fontSize } from "@mui/system";

type StyleProps = {
    top?: string;
    left?: string;}


export const TextBox = styled.div`

margin-left:"556px"
margin-top: "250px"

box-shadow: 10px 10px 10px rgba(0, 0, 0, 0.25);
border-radius: 2px;

`;